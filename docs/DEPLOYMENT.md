# Deployment

GitHub Pages source: GitHub Actions  
Public root: `public/`  
Initial deployment trigger: 2026-07-24

The deployment workflow validates all structured examples, rebuilds the generated reference sites, uploads `public/` as the Pages artifact, and deploys it to the `github-pages` environment.
